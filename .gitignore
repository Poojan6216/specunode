__pycache__/
*.py[cod]
.venv/
venv/
dist/
build/
*.egg-info/
.mypy_cache/
.ruff_cache/
.pytest_cache/
.hypothesis/
.specunode/
.DS_Store
bench/corpus/raw/
*.db
*.db-wal
*.db-shm
.specunode-chaos/
.env
*.env
# Undigested corpus values: too large to commit, rebuilt with
# `python bench/corpus/fetch.py --values --full`.
bench/corpus/values_full.json
bench/corpus/values_full_manifest.json
# The project reports are for us, not for the public repository -- except the generated
# results report, which bench/make_report_pdf.py builds from bench/results/ alone.
/SpecuNode-Report*.pdf
!/SpecuNode-Report-generated.pdf

# Per-run locks next to a SQLite journal (Journal.hold_run).
*.db.locks/
